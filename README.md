MNIST handwritten digit dataset

full classification pipeline — train/test split i trained SGD classifier compared it with dummy classif. cross-validation evaluated precisiion, recall and F1-score analyzed confusion matrices compared ROC curves snd roc-auc scrs multiclass classification OvO vs OvR
